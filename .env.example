FLASK_ENV=development
SECRET_KEY=change-me
DATABASE_URL=sqlite:///rainbow_safe.db

# Optional: SMTP to send real email alerts (otherwise alerts are logged to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Rainbow Safe <no-reply@localhost>

# Admin (local)
ADMIN_PASSWORD=troque-por-uma-senha-forte
